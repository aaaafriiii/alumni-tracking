{
    "version": 2,
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": ["alumni.db"]
            }
        }
    ],
    "rewrites": [
        { "source": "/(.*)", "destination": "/server.js" }
    ]
}